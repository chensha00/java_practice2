<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.4//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zyht.domain.OrderDetail">

    <!-- 定义结果集-->
    <resultMap type="com.zyht.domain.OrderDetail" id="orderDetailMyBatis">
        <id property="id" column="ID" jdbcType="BIGINT" javaType="java.lang.Long"/>
        <result property="sellerId" column="SELLER_ID" jdbcType="BIGINT"/>
        <result property="buyerId" column="BUYER_ID" jdbcType="BIGINT"/>
        <result property="orderId" column="ORDER_ID" jdbcType="BIGINT"/>
        <result property="amount" column="AMOUNT" jdbcType="DECIMAL"/>
        <result property="isSuccess" column="IS_SUCCESS" javaType="java.lang.Boolean"/>
        <result property="orderStatus" column="ORDER_STATUS" jdbcType="TINYINT"/>
        <result property="orderNumber" column="ORDER_NUMBER" jdbcType="VARCHAR"/>
        <result property="creationTime" column="CREATION_TIME" jdbcType="DATE"/>
        <result property="finishTime" column="FINISH_TIME" jdbcType="DATE"/>
        <result property="physicalDistribution" column="PHYSICAL_DISTRIBUTION" jdbcType="java.lang.String"/>
        <result property="evaluate" column="EVALUATE" jdbcType="java.lang.String"/>
        <result property="leaveWord" column="LEAVE_WORD" jdbcType="java.lang.String"/>
    </resultMap>
    <!--定义查询列 -->
    <sql id="columns">
     ID,SELLER_ID,BUYER_ID,ORDER_ID,AMOUNT,IS_SUCCESS,ORDER_STATUS,ORDER_NUMBER,CREATION_TIME,FINISH_TIME,PHYSICAL_DISTRIBUTION,EVALUATE,LEAVE_WORD
    </sql>
    <!-- 查询所有-->
    <select id="selectAll" resultType="java.util.Map" resultMap="orderDetailMyBatis">
        SELECT <include refid="columns"/> FROM order_detail
    </select>
    <!--按照id查询-->
    <select id="getById" resultType="domain.OrderDetail">
        SELECT <include refid="columns"/> FROM order_detail WHERE id=${id}
    </select>
    <!-- 按条件查询-->
    <select id="queryCondition" resultType="java.util.List" parameterType="java.util.Map" resultMap="test">
        SELECT <include refid="columns"/> FROM order_detail WHERE ${key} = #{VALUE };
    </select>
    <!--按条件分页查询-->
    <select id="queryConditionPage" resultType="java.util.List" parameterType="java.util.Map" resultMap="test">
        SELECT <include refid="columns"/> FROM order_detail WHERE ${key} = #{VALUE } LIMIT 0,5;
    </select>

    <!-- 添加-->
    <insert id="insert" parameterType="domain.OrderDetail">
        INSERT INTO `order_detail` (`ID`, `SELLER_ID`,`BUYER_ID`,`ORDER_ID`,`AMOUNT`,`IS_SUCCESS`,`ORDER_STATUS`,`ORDER_NUMBER`,`CREATION_TIME`,`FINISH_TIME`,PHYSICAL_DISTRIBUTION,EVALUATE,LEAVE_WORD)
        VALUES (${id}, #{sellerId,jdbcType=Long},#{buyerId,jdbcType=Long},#{orderId,jdbcType=Long},#{orderId,jdbcType=Long},#{amount,jdbcType=Double}
        #{isSuccess,jdbcType=Boolean},#{orderStatus,jdbcType=Byte},#{orderNumber,jdbcType=String},,#{creationTime,jdbcType=String},#{finishTime,jdbcType=String},#{physicalDistribution},#{evaluate},#{leaveWord})
    </insert>
    <!-- 更新-->
    <update id="update" parameterType="domain.OrderDetail">
        UPDATE `order_detail` SET `id`=${id},`sellerId`=${sellerId},`buyerId`=${buyerId},`orderId`=${orderId},`amount`=${amount},`isSuccess`=${isSuccess},`orderStatus`=${orderStatus}
        `orderNumber`=${orderNumber},`creationTime`=${creationTime},`finishTime`=${finishTime},physicalDistribution=${physicalDistribution},evaluate=${evaluate},leaveWord=${leaveWord}
        <if test="@Ognl@isNotEmpty(id)">
            WHERE (`id`=${id})
        </if>
        <!--原样输出-->
        <![CDATA[
        ]]>
    </update>
    <!--删除-->
    <delete id="delete" parameterType="java.util.Arrays">
        DELETE FROM  order_detail WHERE  id IN
        <if test="@Ognl@isNotEmpty(ids)">
            <foreach collection="ids" item="orderDetailId" open="("
                     separator="," close=")">
                ${testId}
            </foreach>
        </if>
    </delete>
</mapper>